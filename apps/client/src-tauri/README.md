# Tauri wrapper

The web client is the single UI source. Add the standard Tauri shell here after the web MVP is stable; it should load the built `apps/client/dist` directory and use the same API URL configuration.
